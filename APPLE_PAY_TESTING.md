# בדיקת קליטת Apple Pay — מדריך מלא

## מה המנגנון בפועל

**אין API של Apple Pay.** אפל לא נותנת לאף אפליקציה גישה תכנותית לעסקאות שלך.
המנגנון היחיד הוא **Personal Automation באפליקציית Shortcuts** — טריגר בשם
"Transaction" (ב-iOS 26 שונה השם ל-"Wallet"), שמריץ קיצור דרך אחרי תשלום בכרטיס
שבחרת ב-Wallet, ומעביר שני משתנים: **Amount** ו-**Merchant**.

האפליקציה חושפת App Intent בשם `RecordTransactionIntent` שמקבל אותם.

### שלוש מגבלות שחייבים לדעת מראש

| מגבלה | משמעות |
|---|---|
| **עובד רק בתשלום פיזי (tap)** | רכישות אונליין ובתוך אפליקציות **לא** מפעילות את האוטומציה |
| **הטריגר רץ גם על עסקה שנדחתה** | תשלום שנכשל עלול להירשם כהוצאה |
| **מטען פגום מדי פעם** | באג פתוח אצל אפל: merchant מגיע ריק ו-amount מגיע `0.0` |

המגבלה הראשונה היא מוצרית ואי אפשר לעקוף אותה. השתיים האחרות — הקוד עכשיו
מוגן מפניהן (`TransactionIngest`), אבל צריך לוודא שההגנה באמת עובדת אצלך.

---

## הגדרת האוטומציה

**דרישת חובה מוקדמת:** Settings → Apps → Wallet → **Mobile Data מופעל**.
בלי זה האוטומציה נכשלת בשקט.

1. Shortcuts → לשונית **Automation** → **+**
2. לבחור **Transaction** (או **Wallet** ב-iOS 26+)
3. **Card**: לבחור את הכרטיס (או All Cards)
4. **Run Immediately** ✅ · **Notify When Run** ❌
   (בלי "Run Immediately" תצטרך לאשר כל תשלום ידנית — זה הורג את הרעיון)
5. **Next** → **New Blank Automation**
6. להוסיף את הפעולה **הקלטת עסקת Apple Pay** (מ-Money City)
7. למפות את השדות:
   - `סכום העסקה` ← המשתנה **Amount**
   - `שם בית העסק` ← המשתנה **Merchant**
   - `סכום כטקסט` ← המשתנה **Amount** *שוב* (זה הגיבוי לבאג של אפל — **אל תדלג**)
   - `מטבע` ← להשאיר ריק (נלקח מההגדרות באפליקציה)
   - `תאריך ושעה` ← להשאיר ריק (נלקח שעון המכשיר)

---

## שלוש רמות בדיקה

### רמה 1 — מסלול הקליטה, בלי לצאת מהאפליקציה

הכי מהיר, ומכסה הכל חוץ מההעברה של Wallet ל-Shortcuts.

באפליקציה: כפתור **+** האדום → למטה, **"בדיקת מסלול הקליטה האמיתי"**.
הצ'יפים שם מריצים את `RecordTransactionIntent` **עצמו** — אותו קוד בדיוק
שהאוטומציה מריצה.

| מה ללחוץ | תוצאה תקינה |
|---|---|
| "Wolt משלוחים ₪85" | ✅ נקלט · בעיר נבנה מבנה משלוחים · מופיע בהיסטוריה |
| ללחוץ שוב מיד | ❌ "נדחה: כפילות בתוך 90 שניות" |
| 🧪 "מטען ריק" | ❌ "נדחה: אין שם בית עסק" — **ולא נוצרה עסקת ₪0** |
| 🧪 "סכום כטקסט 24,00" | ✅ נקלט כ-₪24.00 (הגיבוי לבאג של אפל עובד) |
| "עסק לא מוכר" כלשהו | ✅ נקלט, ומסומן **"סיווג לא ודאי"** בהיסטוריה |

אם אחד מהאדומים למעלה **כן** יוצר עסקה — יש רגרסיה בהגנות.

### רמה 2 — הקיצור עצמו, בלי לבזבז כסף

בודק שהמיפוי ב-Shortcuts נכון.

1. Shortcuts → **My Shortcuts** → קיצור דרך חדש
2. להוסיף את הפעולה **הקלטת עסקת Apple Pay**
3. להקליד ידנית: סכום `24`, בית עסק `ארומה קפה`
4. ▶️ הרצה

צריך לקבל דיאלוג "נרשמה עסקה ע״ס ₪24.00 ב-ארומה קפה (אוכל)", ולראות אותה
באפליקציה. אם כאן זה עובד אבל האוטומציה לא — הבעיה במיפוי המשתנים, לא בקוד.

### רמה 3 — עסקה אמיתית

הבדיקה היחידה שבאמת סוגרת את הלולאה, ואי אפשר לדלג עליה.

1. לקנות משהו **בחנות פיזית ב-tap** — קפה, בקבוק מים. ₪5 מספיקים.
2. תוך שניות: לפתוח את Money City ולבדוק שהעסקה שם.
3. לחזור על זה **בשלושה בתי עסק שונים** — הבאג של המטען הריק לא עקבי,
   בדיקה אחת לא מוכיחה כלום.

**חשוב:** לבדוק גם כשהאפליקציה **סגורה לגמרי** (swipe up מה-App Switcher).
זה המצב האמיתי — האינטנט צריך להעיר את האפליקציה ברקע.

---

## אבחון תקלות

| תסמין | סיבה סבירה | מה לעשות |
|---|---|---|
| שום דבר לא נקלט | Mobile Data כבוי ל-Wallet | Settings → Apps → Wallet |
| האוטומציה מבקשת אישור | "Run Immediately" כבוי | לערוך את האוטומציה |
| נקלט ₪0 / בלי שם | הבאג של אפל | ההגנה אמורה לדחות — לוודא שמיפית גם `סכום כטקסט` |
| נקלט פעמיים | ריטריי של האוטומציה | חלון הכפילות (90 שניות) אמור לחסום |
| רכישות אונליין לא נקלטות | **מגבלת מערכת** | להזין ידנית דרך כפתור ה-+ |
| עסקה שנדחתה נרשמה | **מגבלת מערכת** | למחוק מההיסטוריה (לחיצה ארוכה) |
| הסיווג שגוי | ניחוש של מנוע המילים | לחיצה ארוכה בהיסטוריה → תיקון קטגוריה |

## טסטים אוטומטיים

```bash
swift test
```

`MoneyCityTests/TransactionIngestTests.swift` מכסה את כל מקרי הקצה למעלה —
מטען ריק, סכום 0, סכום כטקסט בפורמטים שונים, כפילויות, וסיווג בביטחון נמוך.
תריץ את זה אחרי כל שינוי ב-`TransactionIngest` או ב-`CategorizationEngine`.

---

# הוצאות קבועות — הצד השני של הכיסוי

האוטומציה תופסת את העסקאות הקטנות והתכופות. ההוצאות הגדולות והקבועות
(שכר דירה, ארנונה, מנויים) לא מגיעות אליה לעולם — והן גם אלה שאם תשכח
להזין חודש אחד, פארק החיסכון יתנפח והעיר תשקר לך.

**הגדרות → הוצאות קבועות** → הוספה. שם, סכום, יום בחודש, קטגוריה.
מכאן זה נרשם לבד בכל חודש.

## מה לבדוק

| מקרה | תוצאה תקינה |
|---|---|
| הוספת "שכר דירה" ליום 1, כשהיום ה-20 | נוצרת מיד עבור החודש הנוכחי |
| הוספת הוצאה ליום 28, כשהיום ה-5 | **לא** נוצרת עדיין |
| לסגור את האפליקציה לחודשיים ולפתוח | נוצרות שתי ההוצאות שנפספסו |
| הוצאה ליום 31, בפברואר | נרשמת ב-28 (או 29 בשנה מעוברת) |
| להזין ידנית "שכר דירה" ואז להגדיר תבנית באותו שם | **לא** נוצרת כפילות |
| "השהה" בלחיצה ארוכה | מפסיקה להיווצר, נשמרת ברשימה |

`MoneyCityTests/RecurringExpenseTests.swift` מכסה את כל אלה. הלוגיקה
מקבלת `Calendar` כפרמטר, כך שהטסטים לא תלויים באזור הזמן של המכשיר.
