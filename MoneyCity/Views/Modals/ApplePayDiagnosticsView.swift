//  Removed before the first App Store release.
//
//  The Apple Pay diagnostics / simulation console lived here.
//  It could write fabricated transactions into the real store and post a notification branded
//  "Apple Pay", and its hardcoded Hebrew merchant names and amounts were compiled into the
//  shipping binary even while the screen was unreachable.
//
//  The file is left empty rather than deleted because sources are listed explicitly in
//  project.pbxproj — remove it from the Xcode navigator to drop it from the project properly.
