# 📱 ארכיטקטורת מסכים וממשק משתמש (UI/UX Spec)

---

## 1. מבנה האפליקציה (Information Architecture)

האפליקציה בנויה מ-2 מסכי עוגן מרכזיים ומסכי תמיכה מודאליים (Sheets):

```
┌────────────────────────────────────────────────────────┐
│                        MAIN APP                        │
├──────────────────────────┬─────────────────────────────┤
│ 🏙️ טאב 1: העיר (Diorama)  │ 📜 טאב 2: יומן עסקאות (Feed)│
└─────────────┬────────────┴──────────────┬──────────────┘
              │                           │
       [+] Quick Add                [Edit Modal]
       (מזומן / ביט)             (שינוי קטגוריה בטאפ)
              │
       [Time Travel]
     (סליידר חודשים / שנתי)
```

---

## 2. פירוט מסכי המפתח

### מסך 1: מסך הבית — העיר החיה (The Living Diorama)
* **Header עליון:**
  * בורר חודש: `‹ אוגוסט 2026 ›` (חצים או מחוות Swipe).
  * כפתור מעבר למבט שנתי: `[ 🗺️ שנה שלמה ]`.
* **מרכז המסך — קנבס העיר (Interactive Canvas):**
  * דיורמה איזומטרית (2.5D) מרהיבה.
  * שטחים ירוקים (חיסכון) מול שכונות עירוניות (הוצאות).
  * לחיצה על אזור/מבנה בעיר: מבליטה את ההוצאות הרלוונטיות (למשל: לחיצה על המסעדה מקפיצה "🍔 אוכל: ₪1,240 — 8 עסקאות").
  * תאורה דינמית: מצב יום / לילה לפי השעה האמיתית.
* **כרטיס תחתון (Bottom Summary Card):**
  * סך הוצאות חודשי: `₪4,860` (בפונט נקי ומודרני).
  * שורת צ'יפים של קטגוריות עם אחוזים וסכום: `[🍔 ₪1.8K] [☕ ₪240] [🚗 ₪620] [🌱 ₪2.1K]`.
* **כפתור + צף (FAB - Quick Add):**
  * ממוקם בפינה התחתונה לצורך הוספה מהירה של מזומן.

---

### מסך 2: הוספה מהירה ב-2 שניות (Quick Add Modal)
* מופעל בלחיצה על כפתור ה-`+`.
* **מבנה:**
  1. מקלדת מספרים גדולה להזנת סכום (למשל: `4` `5` ➔ `₪45`).
  2. שורת אייקונים/צ'יפים לבחירה מיידית בטאפ אחד (`☕ קפה`, `🍔 אוכל`, `🛒 קניות`, `🚗 תחבורה` וכו').
  3. **Auto-Submit:** לחיצה על הקטגוריה שומרת את העסקה מיד, סוגרת את המסך ומוסיפה את המבנה לעיר (עם אפקט Pop נעים).
  4. (אופציונלי): שדה טקסט קצר לתיאור/הערה ("שווארמה עם חברים").

---

### מסך 3: יומן עסקאות והיסטוריה (Transactions & Learning Feed)
* רשימה כרונולוגית נקייה המחולקת לפי ימים:
  * **היום**
    * ☕ ארומה תל אביב • `14:20` ➔ `₪18.00` `[קפה ✓]`
    * 🍔 Wolt • `12:45` ➔ `₪76.00` `[אוכל ✓]`
  * **אתמול**
    * 🛒 שופרסל דיל • `19:10` ➔ `₪214.50` `[קניות ✓]`
    * 🚕 Gett • `08:30` ➔ `₪42.00` `[תחבורה ✓]`
* **אינטראקציה:**
  * לחיצה על כל שורה פותחת Sheet קטן לשינוי קטגוריה בטאפ אחד.
  * במידה והקטגוריה שונתה ➔ המערכת שואלת: *"לזכור את זה תמיד עבור [שם העסק]?"*

---

### מסך 4: מסע בזמן ומבט שנתי (Time Travel & Annual Metropolis)
* **סליידר חודשים תחתון:**
  * גרירת הסליידר מעבירה בצורה חלקה (Morphing / Fade) בין עיר ינואר, פברואר, מרץ... וכו'.
* **תצוגת מטרופולין (12 Months Overview):**
  * כל חודשי השנה מוצגים כאיים / רובעים מחוברים ברשת כבישים וגשרים.
  * מאפשר לראות את ההתפתחות השנתית כמפה אחת ענקית.

---

### מסך 5: ה-Onboarding והגדרת ה-Shortcuts
1. **Welcome Screen:** "הכסף שלך בונה עיר. בלי בנקים. בלי הקלדות מיותרות."
2. **Setup Wizard:**
   * אנימציה קצרה של 3 צעדים המדגימה איך להפעיל את ה-Personal Automation באפליקציית ה-Shortcuts של אפל.
   * כפתור ראשי: **"פתח את Shortcuts להגדרה"**.
3. **Test Transaction ("זרע ראשון"):**
   * כפתור: **"שלח עסקת הדגמה (☕ ₪15 בארומה)"**.
   * המשתמש לוחץ ➔ העיר החודשית נפתחת עם המבנה הראשון!
